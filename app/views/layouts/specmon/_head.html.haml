%head
  %meta{name: "viewport", content: "width=device-width, initial-scale=1, maximum-scale=1"}
  %title= content_for?(:title) ? yield(:title) : "Specmon"
  %meta{content: content_for?(:description) ? yield(:description) : "Specmon", name: "Specmon"}
  = csrf_meta_tags
  = yield(:head)
